{
  "scripts": {
    "asbuild:untouched": "asc assembly/index.ts -b build/untouched.wasm -t build/untouched.wat --sourceMap --debug",
    "asbuild:optimized": "asc assembly/index.ts -b build/optimized.wasm -t build/optimized.wat -O3 --exportRuntime --noAssert",
    "asbuild": "npm run asbuild:untouched && npm run asbuild:optimized",
    "dev-js": "yarn asbuild && cp build/*.map dist/ && cp build/*.wasm dist/ && cp src/*.jpg dist/ && parcel src/index.js.html",
    "dev-as": "yarn asbuild && cp build/*.map dist/ && cp build/*.wasm dist/ && cp src/*.jpg dist/ && parcel src/index.as.html",
    "deploy": "parcel build  --public-url ./ src/index.html && git add dist && git commit -m \"auto deploy\" && git subtree push --prefix dist origin gh-pages"
  },
  "devDependencies": {
    "assemblyscript": "0.18.26",
    "parcel": "^1.12.4",
    "parcel-bundler": "^1.12.3",
    "typescript": "^3.7.5"
  },
  "dependencies": {
    "@assemblyscript/loader": "0.18.26"
  }
}
